{{define "content"}}
{{$d := .Data}}
<h1>Overview</h1>
<p class="subtitle">Queues and recent activity, updating live.</p>

<section class="tiles" data-poll="tiles">
  {{range $d.Tiles}}
  <div class="tile">
    <div class="label"><span class="sdot {{.Class}}"></span>{{.Label}}</div>
    <div class="value">{{.Count}}</div>
  </div>
  {{end}}
</section>

<section>
  <h2>Queues</h2>
  <div class="card" data-poll="queues">
    {{if $d.Queues}}
      {{range $d.Queues}}
      <div class="queue-row">
        <div class="queue-head">
          <span class="queue-name">{{.Name}}</span>
          <span class="queue-total">{{.Total}} total</span>
        </div>
        {{if .Segments}}
        <div class="depth-bar">
          {{range .Segments}}<span class="{{.Class}}" style="flex: {{.Count}}" title="{{.Count}} {{.Label}}"></span>{{end}}
        </div>
        {{end}}
        <div class="queue-counts">
          {{range .Counts}}<span><span class="sdot {{.Class}}"></span>{{.Count}} {{.Label}}</span>{{end}}
        </div>
      </div>
      {{end}}
    {{else}}
    <div class="empty">No jobs yet. Enqueue one with <span class="mono">workrail enqueue</span>.</div>
    {{end}}
  </div>
</section>

<section>
  <h2>Recent jobs</h2>
  <div class="card" data-poll="recent">
    {{if $d.Jobs}}
    {{template "jobtable" $d.Jobs}}
    {{else}}
    <div class="empty">Nothing here yet.</div>
    {{end}}
  </div>
</section>
{{end}}
